One-line: what a list shows before there is data.

```jsx
<EmptyState title="No time logged yet" body="Start a timer, or add an entry by hand." action={<Button variant="accent" icon="Play">Start timer</Button>} />
```
