One-line: confirms a completed action and offers undo; never used for errors that need a decision (use Dialog).

```jsx
<ToastStack><Toast icon="Check" actionLabel="Undo" onAction={fn}>Entry saved · 2.25h to Northwind</Toast></ToastStack>
```
