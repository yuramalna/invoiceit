One-line: filters one list in place; never used for top-level navigation (that's SidebarNav).

```jsx
<Tabs value="unbilled" onChange={fn} tabs={[{value:'all',label:'All',count:128},{value:'unbilled',label:'Unbilled',count:14}]} />
```
