One-line: text/number entry; always inside a `Field` unless it lives in a table row.

```jsx
<Input icon="Search" placeholder="Search entries" />
<Input numeric align="right" suffix="h" defaultValue="2.50" />
<Input seamless placeholder="What did you work on?" />
```

Notes: `numeric` switches to IBM Plex Mono with tabular figures — required for any duration or money value.
