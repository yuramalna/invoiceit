One-line: icon-only action, used at the trailing edge of table rows and inside panel headers.

```jsx
<IconButton icon="Play" label="Resume entry" />
<IconButton icon="Trash2" label="Delete" tone="danger" size="sm" />
```

Notes: always pass `label`. Default is chromeless (hover reveals a paper fill); `outlined` for standalone placement.
