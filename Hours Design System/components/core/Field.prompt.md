One-line: wraps every input so labels, hints and errors stay consistent.

```jsx
<Field label="Hourly rate" hint="Applied to new entries only">
  <Input prefix="$" defaultValue="95.00" align="right" />
</Field>
```
