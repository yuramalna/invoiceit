One-line: settings that apply immediately, e.g. "Round entries to 15 min".

```jsx
<Switch checked label="Auto-stop timer at midnight" onChange={fn} />
```

Notes: for form values that need saving, use `Checkbox` instead.
